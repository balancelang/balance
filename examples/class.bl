var cls = new MyClass()
cls.setA(25)
print(cls.getA())
// outputs 25


// We can reference the class both before and after its declaration
class MyClass {
    Int a

    getA(): Int {
        return a
    }

    setA(Int value): None {
        a = value
    }
}